# Insight

`insight` is an skill for extracting a small number of genuinely non-obvious, consequential insights from source material. It prioritizes grounded analysis, precise source anchors, explicit boundaries, and returning no result when the evidence does not clear the bar.

## Usage

Just point it at a source (e.g. book, article, youtube video, podcast) that you want to extract insights from.
